const TOKEN = process.env.TOKEN;
const URL = process.env.URL;
// const LIVE = process.env.LIVE; // TODO: live doesn't work on heroku due to a puppeteer related error

module.exports = {
    TOKEN,
    URL,
    // LIVE
}